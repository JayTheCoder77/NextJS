const Login = () => {
  return <div>Login</div>;
};
export default Login;

// we have defined (auth) in parantheses so that route is skipped

// so we just write localhost:3000/login and not localhost:3000/auth/login
